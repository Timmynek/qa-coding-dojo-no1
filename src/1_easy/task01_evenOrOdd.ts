/**
 * Task 1: 🟢 Even or Odd (5-10 min)
 * 
 * Implement a function that determines if a number is even or odd.
 * 
 * Requirements:
 * - Return "even" for even numbers
 * - Return "odd" for odd numbers
 * - Handle negative numbers
 * - Zero is considered even
 * 
 * Test cases:
 * - evenOrOdd(4) → "even"
 * - evenOrOdd(7) → "odd"
 * - evenOrOdd(0) → "even"
 * - evenOrOdd(-3) → "odd"
 */
export function evenOrOdd(n: number): string {
    return ""; 
}


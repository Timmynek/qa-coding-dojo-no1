/**
 * Task 4: 🟢 Count Character (10 min)
 * 
 * Implement a function that counts occurrences of a character in a string.
 * 
 * Requirements:
 * - Count how many times a character appears in the string
 * - Return 0 for empty strings
 * - Case-sensitive matching
 * 
 * Test cases:
 * - countChar("hello", "l") → 2
 * - countChar("", "a") → 0
 * - countChar("aaaa", "a") → 4
 */
export function countChar(str: string, char: string): number {
    return 0;
}


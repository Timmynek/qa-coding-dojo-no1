/**
 * Task 5: 🟢 Max Number (10 min)
 * 
 * Implement a function that finds the maximum number in an array.
 * 
 * Requirements:
 * - Return the largest number in the array
 * - Handle negative numbers
 * - Handle arrays with single element
 * 
 * Test cases:
 * - maxNumber([3,7,2,9,5]) → 9
 * - maxNumber([-5,-1,-10]) → -1
 * - maxNumber([42]) → 42
 */
export function maxNumber(arr: number[]): number {
    return 0;
}


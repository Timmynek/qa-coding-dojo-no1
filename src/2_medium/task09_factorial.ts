/**
 * Task 9: 🟠 Factorial (10-15 min)
 * 
 * Implement a function that calculates the factorial of a number.
 * 
 * Requirements:
 * - Return the factorial (n!)
 * - Factorial: n! = n × (n-1) × (n-2) × ... × 1
 * - 0! = 1 (by definition)
 * - 1! = 1
 * 
 * Test cases:
 * - factorial(5) → 120 (5 × 4 × 3 × 2 × 1)
 * - factorial(0) → 1
 * - factorial(1) → 1
 */
export function factorial(n: number): number {
    return 1;
}


/**
 * Task 14: 🟠 Merge Arrays (10-15 min)
 * 
 * Implement a function that merges two arrays and removes duplicates.
 * 
 * Requirements:
 * - Combine both arrays into one
 * - Remove duplicate values
 * - Order doesn't matter but should be consistent
 * - Handle empty arrays
 * 
 * Test cases:
 * - mergeArrays([1,2], [2,3]) → [1,2,3]
 * - mergeArrays([], []) → []
 */
export function mergeArrays(arr1: number[], arr2: number[]): number[] {
    return [];
}


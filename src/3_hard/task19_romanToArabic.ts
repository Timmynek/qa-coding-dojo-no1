/**
 * Task 19: 🔴 Roman to Arabic (20-25 min)
 * 
 * Implement a function that converts Roman numerals to Arabic numbers.
 * 
 * Requirements:
 * - Convert Roman numerals (I, V, X, L, C, D, M) to integers
 * - Handle subtractive notation (IV=4, IX=9, XL=40, XC=90, CD=400, CM=900)
 * - Roman numeral values: I=1, V=5, X=10, L=50, C=100, D=500, M=1000
 * 
 * Test cases:
 * - romanToArabic("IX") → 9
 * - romanToArabic("XIV") → 14
 * - romanToArabic("MCMXCIV") → 1994
 * - romanToArabic("I") → 1
 * 
 * Hint: When a smaller value appears before a larger value, subtract it
 */
export function romanToArabic(roman: string): number {
    return 0;
}

